<!DOCTYPE html>
<html>
  {{template "base" .}}
  <body>
    <div>
      {{template "header" .}}
    </div>
    <div>
    <div class="card" style="margin: 15px;">
        <div class="card-header text-danger">ページが存在しません。</div>
        <div class="card-body">
          <h5>リンクが間違っています。</h5>
        </div>
      </div>
    </div>
  </body>
</html>